import React from 'react'

const SectionHeading = ({text,fontSize}) => {
  return (
    <div className='heading text-[22px] sm:text-[36px] xl:text-[44px] font-semibold text-headingColor leading-normal'>{text}</div>
  )
}
export default SectionHeading
